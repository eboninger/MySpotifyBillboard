import { RoundTwoDecPipe } from './round-two-dec.pipe';

describe('RoundTwoDecPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundTwoDecPipe();
    expect(pipe).toBeTruthy();
  });
});
